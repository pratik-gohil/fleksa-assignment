import { createSlice } from "@reduxjs/toolkit";

const SLICE_NAME = "addresses"

export const AddressesSlice = createSlice({
  name: SLICE_NAME,
  initialState: {
    
  },
  reducers: {

  },
  extraReducers: {
    
  }
})